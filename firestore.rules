
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * PROTOTYPING RULES
     * Allows all authenticated users to read/write all documents.
     * This ensures the Admin Dashboard, Staff portal, and order management work perfectly.
     */
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

    /**
     * PUBLIC TRACKING RULE
     * Allows anyone to get specific orders for the customer tracking portal.
     */
    match /orders/{orderId} {
      allow get: if true;
    }
    
    /**
     * USER PROFILE RULES
     * Essential for role-based redirection during login.
     */
    match /users/{userId} {
      allow read: if request.auth != null && request.auth.uid == userId;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
