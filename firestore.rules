rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * TESTING MODE RULES
     * Allows all reads and writes for rapid prototyping.
     * Ensure you implement proper role-based access before production.
     */
    match /{document=**} {
      allow read, write: if true;
    }
  }
}